---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 08
type: execute
wave: 7
depends_on: [06-07]
files_modified:
  - src/renderer/src/components/details/DiffModal.tsx
  - src/renderer/src/utils/patchBuilder.ts
  - src/renderer/src/store/useUndoStore.ts
  - e2e/partial-staging.spec.ts
autonomous: true
requirements: [CONFLICT-02]
must_haves:
  truths:
    - "DiffModal uses stable canonical selections and performs one transaction, one refresh, and one notification."
    - "Visible Undo Transaction and Redo restore exact prior/next Git state and explain expiry."
    - "Large diffs avoid unbounded renderer LCS while file/hunk/line/range actions remain usable."
  artifacts:
    - path: src/renderer/src/components/details/DiffModal.tsx
      provides: Canonical batch selection and visible Undo/Redo UI
    - path: e2e/partial-staging.spec.ts
      provides: Atomic, stale, Undo/Redo, one-refresh Electron proof
  key_links:
    - from: src/renderer/src/components/details/DiffModal.tsx
      to: src/main/partialPatchService.ts
      via: Typed preload transaction and Undo/Redo calls
---

<objective>
Migrate the ordinary diff UI to canonical stable selections and user-visible transaction Undo/Redo.

Purpose: Retain granular staging usability while enforcing atomic backend behavior and bounded rendering.
Output: Updated DiffModal/selection utilities, undo integration, and Playwright tests.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-07-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate DiffModal selections, batches, refresh, and Undo/Redo</name>
  <files>src/renderer/src/components/details/DiffModal.tsx, src/renderer/src/utils/patchBuilder.ts, src/renderer/src/store/useUndoStore.ts</files>
  <read_first>
    - src/renderer/src/components/details/DiffModal.tsx
    - src/renderer/src/utils/patchBuilder.ts
    - src/renderer/src/store/useUndoStore.ts
    - src/preload/index.d.ts
  </read_first>
  <behavior>
    - File, hunk, changed-line, contiguous-range, and multi-hunk selections store service-provided stable IDs.
    - Every stage/unstage/discard/staged-discard sends one batch and refreshes once on success or stale rejection.
    - Undo Transaction and Redo are visible, repository-scoped, exact, and disabled with an expiry explanation after external change.
    - Large inputs fall back from expensive inline character highlighting while preserving raw diff and actions.
  </behavior>
  <action>Stop building mutation patches in the renderer; retain patchBuilder only for presentation and pure selection transforms. Replace per-hunk loops and multi-step staged discard with one transaction, one toast, and one refresh. Integrate returned opaque Undo/Redo handles into useUndoStore without storing repository content; render visible Undo Transaction/Redo controls and expiry reason. On STALE_DIFF preserve intent long enough to explain it, reload canonical metadata once, and do not partially update selection. Add explicit line/character-work thresholds to avoid quadratic matrices.</action>
  <acceptance_criteria>
    - No UI action loops to mutate Git or submits patch text.
    - Unmerged files open ConflictWorkbench.
    - Undo/Redo updates UI and exact Git state with one refresh each.
    - Large diff raw content remains accessible.</acceptance_criteria>
  <verify><automated>bun run build && bun test src/renderer/src/store/__tests__/useUndoStore.test.ts</automated></verify>
  <done>DiffModal is canonical-selection driven, single-transaction, single-refresh, bounded, and visibly undoable/redoable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove atomic partial actions and Undo/Redo through Electron</name>
  <files>e2e/partial-staging.spec.ts</files>
  <read_first>
    - e2e/conflict-resolver.spec.ts
    - e2e/helpers/git-sandbox.ts
    - src/renderer/src/components/details/DiffModal.tsx
  </read_first>
  <behavior>
    - File/hunk/line/range/multi-hunk stage, unstage, discard, and staged-discard produce exact expected Git content.
    - Failed preflight and stale external edit leave index/worktree unchanged and refresh once.
    - Undo and Redo restore exact before/after states; external changes expire them visibly.
  </behavior>
  <action>Create real-repository Electron tests with repeated text, multi-hunk batches, CRLF/no-final-newline, and stale external edits. Assert actual index/worktree bytes, action call/refresh counts, toast/error, Undo/Redo controls, restored states, and expiry—not only visual selection.</action>
  <acceptance_criteria>
    - Every ordinary partial action class and batch rollback is tested.
    - Undo/Redo exactness and expiry are Git-verified.
    - No partial mutation occurs on any failed batch.</acceptance_criteria>
  <verify><automated>bunx playwright test e2e/partial-staging.spec.ts && bun test src/main/__tests__/git.partialPatch.test.ts</automated></verify>
  <done>Playwright proves stable, atomic, stale-safe, one-refresh, undoable ordinary partial actions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI selection state to transaction | Stale UI intent may no longer identify the same Git changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-15 | Tampering | DiffModal | mitigate | Stable IDs, stale reload, exact service transaction, no renderer patch. |
| T-06-16 | Denial of service | Highlighting | mitigate | Bounded work thresholds and raw fallback. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bunx playwright test e2e/partial-staging.spec.ts && bun test src/main/__tests__/git.partialPatch.test.ts && bun run build`</verification>
<success_criteria>Users retain granular ordinary diff actions with atomic one-refresh behavior and visible exact Undo/Redo.</success_criteria>
## Artifacts this phase produces
- Canonical-selection DiffModal.
- Partial staging Undo/Redo UI and E2E.
<output>Create `06-08-SUMMARY.md` in this phase directory.</output>
