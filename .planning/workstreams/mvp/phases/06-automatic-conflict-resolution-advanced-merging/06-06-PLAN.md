---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 06
type: execute
wave: 5
depends_on: [06-05]
files_modified:
  - src/renderer/src/components/sidebar/ConflictResolver.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/components/toolbar/Toolbar.tsx
  - src/renderer/src/components/graph/GraphView.tsx
  - e2e/conflict-resolver.spec.ts
  - e2e/rebase-conflict-resolver.spec.ts
autonomous: true
requirements: [CONFLICT-01, CONFLICT-02]
must_haves:
  truths:
    - "App, Toolbar, GraphView, and resolver entry points use one operation controller."
    - "The workbench stays mounted and updates generation across consecutive same-count rebase conflicts."
    - "Real merge, rebase, cherry-pick, structural choices, external changes, and Undo Resolution pass through the Electron UI."
  artifacts:
    - path: e2e/conflict-resolver.spec.ts
      provides: Merge/cherry-pick/structural/manual/undo accessibility workflows
    - path: e2e/rebase-conflict-resolver.spec.ts
      provides: Consecutive rebase-stop and role-label workflow
  key_links:
    - from: src/renderer/src/App.tsx
      to: src/renderer/src/store/useRepoStore.ts
      via: Snapshot generation and centralized actions
---

<objective>
Wire the workbench into every existing operation entry point and prove the complete conflict workflow through real Electron repositories.

Purpose: Remove divergent component-local continuation behavior and establish user-visible safety, navigation, and undo evidence.
Output: Unified App/Toolbar/Graph integration, legacy resolver migration, and focused E2E suites.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-05-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace duplicated operation orchestration</name>
  <files>src/renderer/src/components/sidebar/ConflictResolver.tsx, src/renderer/src/App.tsx, src/renderer/src/components/toolbar/Toolbar.tsx, src/renderer/src/components/graph/GraphView.tsx</files>
  <read_first>
    - src/renderer/src/App.tsx
    - src/renderer/src/components/toolbar/Toolbar.tsx
    - src/renderer/src/components/graph/GraphView.tsx
    - src/renderer/src/components/sidebar/ConflictResolver.tsx
    - src/renderer/src/store/useRepoStore.ts
  </read_first>
  <action>Replace local conflict Sets, count-based effects, and direct continue/skip/abort calls with useRepoStore selectors/actions and ConflictWorkbench. Reduce the legacy resolver to a compatible entry or remove it after all callers migrate. Preserve equivalent stable test IDs. Never clear after one continue; render the returned snapshot and generation. Keep visible Undo Resolution beside Apply/Continue until it expires, and refresh external changes through the controller.</action>
  <acceptance_criteria>
    - No component independently infers operation completion or calls operation IPC directly.
    - Same conflict count with a new generation reloads the workbench.
    - Existing toolbar/banner/graph entry points open the same session.</acceptance_criteria>
  <verify><automated>bun run build</automated></verify>
  <done>All operation surfaces share one authoritative controller and workbench.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real conflict, accessibility, external-change, and undo flows</name>
  <files>e2e/conflict-resolver.spec.ts, e2e/rebase-conflict-resolver.spec.ts</files>
  <read_first>
    - e2e/conflict-resolver.spec.ts
    - e2e/rebase-conflict-resolver.spec.ts
    - e2e/helpers/git-sandbox.ts
  </read_first>
  <behavior>
    - Merge and cherry-pick cover unresolved gating, Base, manual Result, both orders, granular range, structural/delete choice, Apply, Undo Resolution, reapply, and continue.
    - Two-stop rebase covers Onto/Replayed labels, opposite-side expectations, identical conflict counts, generation advance, skip, abort, and final content.
    - Keyboard/focus/ARIA, Escape-to-minimize, external-edit recovery, and Git index/content are asserted.
  </behavior>
  <action>Expand the real GitSandbox Electron tests without mocks. Assert UI and Git state after each mutation, including stage entries and exact bytes. Make the Plan 01 tracked second-stop assertion green. Verify Undo Resolution restores the conflict and Result before Continue, and that it is unavailable after Continue/generation change.</action>
  <acceptance_criteria>
    - All named merge/rebase/cherry-pick and UI behaviors have executable assertions.
    - Undo safety and expiry are visible and Git-verified.
    - No test relies only on toast text or conflict counts.</acceptance_criteria>
  <verify><automated>bunx playwright test e2e/conflict-resolver.spec.ts e2e/rebase-conflict-resolver.spec.ts</automated></verify>
  <done>Real Electron workflows prove operation continuity, accessible control, exact results, and resolution undo.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Multiple UI entry points to controller | Divergent calls could bypass authoritative state or undo expiry. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-12 | Tampering | Entry points | mitigate | Central actions only and real-Git E2E postconditions. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bunx playwright test e2e/conflict-resolver.spec.ts e2e/rebase-conflict-resolver.spec.ts && bun run build`</verification>
<success_criteria>Every existing operation surface uses one safe workflow and real E2E covers repeated conflicts, exact outcomes, accessibility, and undo.</success_criteria>
## Artifacts this phase produces
- Unified operation entry points.
- Conflict/rebase/cherry-pick/undo E2E coverage.
<output>Create `06-06-SUMMARY.md` in this phase directory.</output>
