---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 09
type: execute
wave: 8
depends_on: [06-08]
files_modified:
  - src/shared/conflicts.ts
  - src/main/conflictCandidates.ts
  - src/main/conflictService.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/env.d.ts
  - src/main/__tests__/conflictCandidates.test.ts
autonomous: true
requirements: [CONFLICT-03]
must_haves:
  truths:
    - "Candidate discovery is off by default, repository-local, UltraGIT-owned, and non-mutating across discovery, preview, reject, and process restart."
    - "At minimum one-side-equals-base, identical normalized result, byte-safe whitespace-only, and prior confirmed-record candidates are generated."
    - "Every candidate carries rule/provenance, safety explanation, stage identities, and before/after preview and cannot stage or write."
  artifacts:
    - path: src/main/conflictCandidates.ts
      provides: Deterministic candidate generators and repository-local resolution-record store
    - path: src/main/__tests__/conflictCandidates.test.ts
      provides: Candidate correctness, non-mutation, persistence, safety, and stale tests
  key_links:
    - from: src/main/conflictCandidates.ts
      to: src/main/conflictService.ts
      via: Read-only ConflictDocument and explicit confirmed-result recording after guarded Apply
---

<objective>
Implement CONFLICT-03 candidate generation with deterministic rules and a non-mutating UltraGIT-owned record store.

Purpose: Offer useful automation while guaranteeing that discovery, preview, rejection, and restart cannot change Git state.
Output: Candidate contracts/service, repository-local records, guarded read/config APIs, and real-Git tests.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-08-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic preview-only candidate generators</name>
  <files>src/shared/conflicts.ts, src/main/conflictCandidates.ts, src/main/__tests__/conflictCandidates.test.ts</files>
  <read_first>
    - src/shared/conflicts.ts
    - src/main/conflictRegions.ts
    - src/main/conflictService.ts
    - .planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
  </read_first>
  <behavior>
    - One-side-equals-base proposes the changed side only when exact stage bytes/OIDs establish the unchanged side.
    - Identical normalized result proposes one result only when normalization is declared and byte-level output policy is deterministic.
    - Whitespace-only proposes a result only when non-whitespace tokens/content, EOL policy, encoding, modes, and no-final-newline safety checks pass.
    - Candidate output includes rule ID, rationale, stage identities, affected regions, proposed bytes/hash, and before/after preview without writing.</behavior>
  <action>Create pure candidate generators over authoritative ConflictDocument/ConflictRegions. Implement the three locked deterministic rules with conservative byte/content guards; ambiguous encoding, binary, structural mode, mixed EOL, token/content changes, or unclear final newline yields no whitespace candidate. Candidate IDs bind rule, generation, stage OIDs, region IDs, and proposed hash. Do not apply, stage, resolve, or mutate during generation/preview.</action>
  <acceptance_criteria>
    - Positive and negative fixtures cover every rule, including unsafe whitespace/EOL/final-newline cases.
    - Candidate order is stable and duplicate proposed results deduplicate while retaining provenance.
    - Index/worktree hashes remain unchanged.</acceptance_criteria>
  <verify><automated>bun test src/main/__tests__/conflictCandidates.test.ts</automated></verify>
  <done>All required deterministic candidates are conservative, provenance-rich, stable, and non-mutating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the repository-local confirmed-resolution store and guarded candidate API</name>
  <files>src/main/conflictCandidates.ts, src/main/conflictService.ts, src/main/index.ts, src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/env.d.ts, src/main/__tests__/conflictCandidates.test.ts</files>
  <read_first>
    - src/main/conflictCandidates.ts
    - src/main/conflictService.ts
    - src/main/index.ts
    - src/preload/index.ts
  </read_first>
  <behavior>
    - Reuse is off by default and configuration/records live under the repository Git directory in a versioned UltraGIT-owned store.
    - Only results explicitly applied and human-confirmed are recordable; matching uses base/current/incoming content identities and normalized path-independent conflict identity.
    - Discover, preview, reject, forget, and process restart leave index/worktree hashes unchanged.
    - Accept returns proposed Result content to the renderer but does not write/stage; Apply & Stage remains separate.</behavior>
  <action>Implement an atomically written, schema-versioned, size/count-capped repository-local UltraGIT record store under the resolved Git directory, excluded from commits by location. Never enable or invoke Git rerere in operation commands. Record only after successful explicit Apply and user confirmation, with provenance and identities but no global config. Provide opt-in status, list/generate, preview, reject-for-current-session, and forget APIs. Guard sender/repository/generation/OID/candidate identity; accepting only returns candidate bytes/hash for an editable Result. Add restart tests that destroy/recreate the service and prove unchanged Git state for discovery/preview/reject plus persisted opt-in/records.</action>
  <acceptance_criteria>
    - Global and repository Git rerere settings remain unchanged.
    - Store corruption/oversize fails closed without Git mutation.
    - Stale candidates and records never populate Result.
    - Forget changes only the UltraGIT store and returns refreshed candidate metadata.</acceptance_criteria>
  <verify><automated>bun test src/main/__tests__/conflictCandidates.test.ts src/main/__tests__/conflictService.test.ts && bun run build</automated></verify>
  <done>Confirmed-resolution candidates persist locally and every pre-Apply candidate action is demonstrably non-mutating.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stored record to proposed Result | Old or corrupt local content may be stale or mismatched. |
| Normalization rule to candidate | Unsafe normalization could hide semantic byte differences. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-17 | Spoofing | Candidate provenance | mitigate | Bind rule/record to generation, stage OIDs, regions, hashes, and schema. |
| T-06-18 | Tampering | Record store | mitigate | Git-dir containment, atomic writes, schema/size validation, fail closed. |
| T-06-19 | Tampering | Whitespace candidate | mitigate | Non-whitespace/content/EOL/encoding/final-newline guards and negative tests. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bun test src/main/__tests__/conflictCandidates.test.ts src/main/__tests__/conflictService.test.ts && bun run build`</verification>
<success_criteria>All required deterministic and recorded candidates are repository-local, off by default, non-mutating, stale-safe, and explicit.</success_criteria>
## Artifacts this phase produces
- Deterministic candidate generators.
- UltraGIT-owned confirmed-resolution store.
- Non-mutation/restart tests and guarded APIs.
<output>Create `06-09-SUMMARY.md` in this phase directory.</output>
