---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/shared/conflicts.ts
  - src/main/conflictRegions.ts
  - src/main/__tests__/conflictRegions.test.ts
autonomous: true
requirements: [CONFLICT-02]
must_haves:
  truths:
    - "Text conflicts have stable Git-derived regions bound to generation, stage OIDs, coordinates, and content hashes."
    - "Both ordering choices, insertions, adjacent edits, overlap grouping, EOL, and no-final-newline behavior compose deterministically into a whole-file Result."
    - "Stale region identities are rejected rather than matched heuristically."
  artifacts:
    - path: src/shared/conflicts.ts
      provides: Shared OperationSnapshot, ConflictDocument, ConflictRegion, selection, error, and undo contracts
    - path: src/main/conflictRegions.ts
      provides: Git-derived base-to-side edit scripts, stable region grouping, and whole-file composition
    - path: src/main/__tests__/conflictRegions.test.ts
      provides: Repeated/adjacent/overlap/insertion/add/delete/EOL composition matrix
  key_links:
    - from: src/main/conflictRegions.ts
      to: Git diff
      via: Controlled temporary blob diff scripts for base-to-current and base-to-incoming
    - from: src/main/conflictRegions.ts
      to: src/shared/conflicts.ts
      via: ConflictRegion identity and composition contracts
---

<objective>
Define the cross-process contracts and authoritative stable conflict-region engine before operation or UI work.

Purpose: Ensure granular conflict choices have one owner and cannot depend on renderer LCS, conflict-marker parsing, or unstable line positions.
Output: Shared contracts, main-process region derivation/composition, and exhaustive pure/real-Git tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-01-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define shared operation, conflict-region, and undo contracts</name>
  <files>src/shared/conflicts.ts</files>
  <read_first>
    - src/preload/index.d.ts
    - src/renderer/src/env.d.ts
    - src/main/git.ts
  </read_first>
  <behavior>
    - ConflictRegion identity includes operation generation, stage 1/2/3 OIDs, base/current/incoming ranges, modes, and content hashes.
    - Region choices are unresolved, current, incoming, both-current-first, both-incoming-first, selected range, or manual.
    - ConflictDocument separately represents authoritative stage blobs, whole-file working Result, structural/non-text type, EOL, and final-newline state.
    - Undo entries bind repository, operation generation, before/after identities, safety snapshot handle, and expiry reason.
  </behavior>
  <action>Create one shared contract module for main, preload, and renderer. Define stable error codes for stale generation/OID/region/diff, containment, size, unsupported conflict, preflight, postcondition, and expired undo. Model structural choices separately from text regions. Define OperationSnapshot terminal/next-conflict outcomes and operation-aware roles, including rebase Onto branch versus Replayed commit. Do not model conflict regions as ordinary partial-stage patches.</action>
  <acceptance_criteria>
    - Contracts contain no renderer-only or Node-only implementation types.
    - No hunk/region has a default side.
    - EOL and final-newline metadata are explicit.
  </acceptance_criteria>
  <verify><automated>bun run build</automated></verify>
  <done>All Electron tiers can consume one unambiguous contract for operation, conflict, composition, errors, and undo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and compose stable conflict regions from Git edit scripts</name>
  <files>src/main/conflictRegions.ts, src/main/__tests__/conflictRegions.test.ts</files>
  <read_first>
    - src/shared/conflicts.ts
    - src/main/__tests__/git.patch.test.ts
    - .planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
  </read_first>
  <behavior>
    - Controlled Git diffs produce base-to-current and base-to-incoming edit scripts without mutating the repository.
    - Overlapping or touching edits group into one region; separated edits remain independently selectable.
    - Simultaneous insertions have deterministic current-first and incoming-first results; identical insertions collapse without duplication.
    - Composition preserves selected side bytes, configured EOL, and no-final-newline state and rejects stale IDs/hashes.
  </behavior>
  <action>Implement main-owned derivation using temporary controlled blobs/index or equivalent non-mutating Git diff inputs. Parse canonical Git edit scripts with context and group intervals using documented overlap/touch rules, including zero-width insertions, add/delete, adjacent changes, repeated text, and one-side-empty cases. Build stable IDs from generation, stage OIDs, coordinates, modes, and content hashes. Compose choices into a whole-file Result in base order, with explicit insertion ordering and both-order semantics. Preserve raw bytes/EOL/no-final-newline; use text normalization only for display metadata, never identity or mutation truth.</action>
  <acceptance_criteria>
    - Tests cover repeated identical lines, adjacent and overlapping edits, same-position insertions, adds/deletes, CRLF/LF, and missing final newline.
    - Running derivation changes neither worktree nor index.
    - A changed OID/hash/generation invalidates every affected region selection.
  </acceptance_criteria>
  <verify><automated>bun test src/main/__tests__/conflictRegions.test.ts</automated></verify>
  <done>Stable regions and whole-file composition are deterministic, byte-aware, non-mutating, and fully tested.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git blobs to region IDs | Binary/text bytes become renderer-addressable identities. |
| Region choices to whole-file Result | Stale or ambiguous coordinates could compose unintended content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03 | Tampering | ConflictRegion | mitigate | Bind IDs to generation, OIDs, coordinates, modes, and hashes; reject stale identities. |
| T-06-04 | Tampering | Composition | mitigate | Canonical overlap/touch and insertion-order rules with byte/EOL tests. |
| T-06-SC | Tampering | Packages | accept | No package install is planned. |
</threat_model>

<verification>`bun test src/main/__tests__/conflictRegions.test.ts && bun run build`</verification>
<success_criteria>Stable conflict regions are authoritative, reproducible, stale-safe, and sufficient to compose one whole-file Result.</success_criteria>

## Artifacts this phase produces
- Shared conflict-region and undo contracts.
- Git-derived region engine and composition tests.

<output>Create `06-02-SUMMARY.md` in this phase directory.</output>
