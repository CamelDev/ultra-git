---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - src/renderer/src/components/conflicts/ConflictWorkbench.tsx
  - src/renderer/src/components/conflicts/ConflictFileList.tsx
  - src/renderer/src/components/conflicts/ConflictHunkView.tsx
  - src/renderer/src/components/conflicts/ResolutionEditor.tsx
  - src/renderer/src/components/conflicts/conflict-workbench.css
autonomous: true
requirements: [CONFLICT-02]
must_haves:
  truths:
    - "Users see full paths/status and synchronized Base, Current, Incoming, and editable whole-file Result panes."
    - "All region choices, both orders, manual editing, structural choices, navigation, and Undo Resolution are explicit and accessible."
    - "The workbench remains usable at minimum window size and handles keyboard, focus, ARIA, loading, errors, and external changes."
  artifacts:
    - path: src/renderer/src/components/conflicts/ConflictWorkbench.tsx
      provides: Persistent operation shell and actions
    - path: src/renderer/src/components/conflicts/ResolutionEditor.tsx
      provides: Whole-file editable Result with region-focused controls
    - path: src/renderer/src/components/conflicts/conflict-workbench.css
      provides: Responsive, resizable, focus-visible layout
  key_links:
    - from: src/renderer/src/components/conflicts/ConflictWorkbench.tsx
      to: src/renderer/src/store/conflictSession.ts
      via: Selectors and intent dispatch only
---

<objective>
Implement the dedicated responsive conflict workbench against the safe session contract.

Purpose: Make complex rebase and structural conflicts understandable and controllable without coupling leaf UI to privileged APIs.
Output: Accessible workbench components and responsive styling.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-04-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the operation strip, semantic file queue, and responsive shell</name>
  <files>src/renderer/src/components/conflicts/ConflictWorkbench.tsx, src/renderer/src/components/conflicts/ConflictFileList.tsx, src/renderer/src/components/conflicts/conflict-workbench.css</files>
  <read_first>
    - src/renderer/src/components/sidebar/ConflictResolver.tsx
    - src/renderer/src/components/dialogs/AppDialog.tsx
    - src/renderer/src/store/conflictSession.ts
    - src/renderer/src/assets/main.css
  </read_first>
  <action>Build a persistent dialog/workbench whose operation strip shows kind, source/target identities, rebase step/total, subject, unresolved/staged progress, Abort/Skip/Continue, and visible Undo Resolution before it expires. Build a semantic keyboard-navigable file queue grouped by unresolved/edited/staged with full path, tooltip, conflict type/status, and progress. Add loading/error/empty/external-change recovery states. Implement focus trap/restoration, accessible name/description, visible focus, and Escape-to-minimize without Git mutation. Use resizable regions, minimum sizes, and responsive stacking; do not rely on color alone.</action>
  <acceptance_criteria>
    - Rebase labels are Onto branch and Replayed commit.
    - Continue derives from authoritative canContinue and Undo is visible only while valid.
    - File rows are semantic controls with full-path/status access.
    - Minimum supported window remains navigable.</acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
    <human-check>At minimum window size, resize/stack regions and verify labels, full paths, focus visibility, keyboard queue navigation, and scrolling.</human-check>
  </verify>
  <done>The workbench shell is responsive, operation-aware, semantic, and undo-visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build synchronized sources and editable region-focused Result</name>
  <files>src/renderer/src/components/conflicts/ConflictHunkView.tsx, src/renderer/src/components/conflicts/ResolutionEditor.tsx, src/renderer/src/components/conflicts/ConflictWorkbench.tsx, src/renderer/src/components/conflicts/conflict-workbench.css</files>
  <read_first>
    - src/renderer/src/components/sidebar/ConflictResolver.tsx
    - src/renderer/src/components/details/DiffModal.tsx
    - src/renderer/src/store/conflictSession.ts
  </read_first>
  <behavior>
    - Base/Current/Incoming/Result panes stay line-aligned and scroll-synchronized with line numbers and bounded highlights.
    - Region navigation offers Current, Incoming, both orders, granular range, Manual, Reset, and Apply & Stage summary.
    - Structural/binary/delete/rename/symlink/submodule cases show file-level choices instead of a text editor.
  </behavior>
  <action>Render authoritative sources with operation-aware labels and raw stage details in advanced help. Use a controlled whole-file Result buffer while focusing navigation and actions on ConflictRegions. Add next/previous file and region shortcuts, both orders, granular change/range selection, manual edit/reset, pre-stage diff summary, and specialized structural/non-text choices. Suppress unmodified shortcuts while editing. Preserve compatible test IDs and add behavior-oriented IDs for Base, roles, Result, unresolved count, both orders, structural actions, and Undo Resolution.</action>
  <acceptance_criteria>
    - Opening/viewing cannot resolve a region and Apply stays disabled until explicit outcomes exist.
    - EOL/final-newline indicators are visible when relevant.
    - Large/binary files avoid unbounded highlighting and use typed fallback.</acceptance_criteria>
  <verify><automated>bun run build && bun test src/renderer/src/store/__tests__/conflictSession.test.ts</automated></verify>
  <done>Users can inspect and compose every supported text or structural conflict explicitly in an accessible Result workflow.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI intent to controller | Accidental shortcuts or misleading side labels may select unintended content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-10 | Tampering | Choice UI | mitigate | Explicit unresolved state, operation roles, provenance, summary, and disabled Apply. |
| T-06-11 | Denial of service | Panes | mitigate | Size thresholds and bounded highlights. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bun run build && bun test src/renderer/src/store/__tests__/conflictSession.test.ts` plus the named human layout check.</verification>
<success_criteria>The workbench covers the complete responsive, accessible, operation-aware UX contract without hidden resolution defaults.</success_criteria>
## Artifacts this phase produces
- Responsive workbench shell/file queue.
- Synchronized source panes and editable Result.
<output>Create `06-05-SUMMARY.md` in this phase directory.</output>
