---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/main/git.ts
  - src/main/__tests__/git.rebase-status.test.ts
  - src/main/__tests__/conflictService.test.ts
  - e2e/helpers/git-sandbox.ts
  - e2e/rebase-conflict-resolver.spec.ts
autonomous: true
requirements:
  - CONFLICT-01
  - CONFLICT-02
must_haves:
  truths:
    - "Combined Bun test runs are trustworthy and no rebase-status mock contaminates real-repository suites."
    - "The current merge, rebase, cherry-pick, abort, and conflict edge-case behavior is characterized before production behavior changes."
    - "A two-stop rebase fixture proves that identical conflict counts can occur in consecutive generations."
  artifacts:
    - path: "src/main/__tests__/conflictService.test.ts"
      provides: "Real-repository operation and conflict characterization matrix"
    - path: "e2e/rebase-conflict-resolver.spec.ts"
      provides: "Repeatable consecutive-rebase-stop Electron fixture"
    - path: "src/main/__tests__/git.rebase-status.test.ts"
      provides: "Isolated rebase metadata tests without process-wide module replacement"
  key_links:
    - from: "src/main/__tests__/git.rebase-status.test.ts"
      to: "src/main/git.ts"
      via: "Injected command/filesystem seam or exported pure metadata parser"
    - from: "e2e/rebase-conflict-resolver.spec.ts"
      to: "e2e/helpers/git-sandbox.ts"
      via: "Real repository with two replayed commits that conflict consecutively"
---

<objective>
Create the trustworthy characterization and test-isolation baseline required before changing conflict semantics.

Purpose: Preserve known-good Git behavior, expose the consecutive-rebase-stop failure deterministically, and ensure later safety work is judged by combined test runs rather than isolated suites.
Output: Isolated Bun tests, a real-repository conflict matrix, and a reusable two-stop rebase E2E fixture.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@GEMINI.md
@.planning/workstreams/mvp/ROADMAP.md
@.planning/workstreams/mvp/REQUIREMENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-VALIDATION.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove cross-suite Git mock contamination</name>
  <files>src/main/git.ts, src/main/__tests__/git.rebase-status.test.ts</files>
  <read_first>
    - src/main/git.ts
    - src/main/__tests__/git.rebase-status.test.ts
    - src/main/__tests__/git.patch.test.ts
  </read_first>
  <behavior>
    - Running the rebase-status and real patch suites together produces the same results as running either suite alone.
    - Rebase metadata parsing remains testable without replacing the simple-git module globally.
  </behavior>
  <action>Extract the smallest pure rebase metadata parser or inject a narrow command/filesystem adapter into the existing status path, then rewrite the test to use that seam instead of process-wide module replacement. Keep production behavior unchanged. Run the combined suite before and after the change and retain a regression assertion proving test order cannot replace the real Git adapter.</action>
  <acceptance_criteria>
    - The test file does not install a global simple-git module mock.
    - Rebase status coverage still includes rebase-merge and rebase-apply metadata.
    - Real patch integration tests pass in the same Bun invocation.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/main/__tests__/git.rebase-status.test.ts src/main/__tests__/git.patch.test.ts</automated>
  </verify>
  <done>Combined execution is green and no test result depends on file ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize authoritative operation and conflict fixtures</name>
  <files>src/main/__tests__/conflictService.test.ts</files>
  <read_first>
    - src/main/__tests__/git.patch.test.ts
    - src/main/git.ts
    - .planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
  </read_first>
  <behavior>
    - Real repositories exercise merge, cherry-pick, one-stop and two-stop rebase, continue, skip, abort, and abort restoration.
    - Fixtures cover both-modified, both-added, modify/delete, unusual and spaced paths, CRLF, binary data, and an external manual edit.
    - Tests record index stages and operation metadata rather than treating marker text as the source of truth.
  </behavior>
  <action>Create a real-repository integration suite using temporary directories and actual Git commands. Add fixture helpers local to the test where possible. Assert current stable behavior and Git facts that the new service must preserve: operation sentinel files, unmerged stage entries, working-tree restoration after abort, and the next stopped commit in a two-stop rebase. Platform-dependent symlink/submodule cases may use explicit capability detection, but must not silently pass when supported.</action>
  <acceptance_criteria>
    - Every required operation and named conflict class has an executable fixture.
    - Cleanup is scoped to each generated temporary repository.
    - Assertions inspect Git state and content, not only renderer-facing booleans.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/main/__tests__/conflictService.test.ts</automated>
  </verify>
  <done>The characterization matrix passes against the pre-refactor service and is suitable for reuse by Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Establish the consecutive rebase-stop Electron fixture</name>
  <files>e2e/helpers/git-sandbox.ts, e2e/rebase-conflict-resolver.spec.ts</files>
  <read_first>
    - e2e/helpers/git-sandbox.ts
    - e2e/conflict-resolver.spec.ts
    - src/renderer/src/App.tsx
  </read_first>
  <behavior>
    - Two replayed commits stop on conflicts with the same conflicted-file count.
    - The fixture can identify the operation step, current subject, conflicted path, and final repository content through real Git.
    - Existing first-stop behavior remains covered while the later plan owns making the second stop stay mounted.
  </behavior>
  <action>Extend GitSandbox only with generally useful commit/rebase fixture helpers, then create the rebase resolver spec with stable setup and Git-state assertions. Mark the precise second-generation UI expectation as the tracked red acceptance case for Plan 06 while keeping fixture creation and first-stop assertions executable; do not hide infrastructure failures behind a broad skip.</action>
  <acceptance_criteria>
    - The fixture deterministically reaches two consecutive conflict stops.
    - The known failure is isolated to the second-generation UI assertion and clearly references the expected operation generation transition.
    - Sandbox cleanup always runs.
  </acceptance_criteria>
  <verify>
    <automated>bunx playwright test e2e/rebase-conflict-resolver.spec.ts --grep "first stop|fixture"</automated>
  </verify>
  <done>The E2E fixture is stable and the exact multi-stop gap is documented by an executable assertion for the later resolver plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process to temporary repositories | Tests execute destructive Git operations and filesystem cleanup against generated paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Test cleanup | mitigate | Create per-test repositories beneath a fixed test directory and delete only the exact generated path. |
| T-06-02 | Repudiation | Characterization results | mitigate | Assert Git-derived state and preserve named cases so behavior changes are attributable. |
| T-06-SC | Tampering | Package installs | accept | No package installation is planned; the existing Bun, Electron, React, Git, and Playwright stack is retained. |
</threat_model>

<verification>
Run the combined Bun suites, the focused rebase fixture checks, and `bun run build`.
</verification>

<success_criteria>
- Combined tests are isolated and deterministic.
- Real repositories characterize all operation classes and critical conflict shapes.
- The multi-stop rebase gap is reproducible without changing production conflict behavior.
</success_criteria>

## Artifacts this phase produces

- A combined-test isolation regression.
- A real-Git operation/conflict characterization matrix.
- A reusable two-stop rebase Electron fixture.

<output>
Create `.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-01-SUMMARY.md` when done.
</output>
