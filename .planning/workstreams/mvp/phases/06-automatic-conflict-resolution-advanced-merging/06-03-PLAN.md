---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - src/main/conflictService.ts
  - src/main/git.ts
  - src/main/index.ts
  - src/main/__tests__/conflictService.test.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/env.d.ts
autonomous: true
requirements: [CONFLICT-01, CONFLICT-02]
must_haves:
  truths:
    - "Every start, resolve, continue, skip, abort, and undo returns the next authoritative OperationSnapshot."
    - "Conflict documents use index stages and stable regions; applying choices stages one whole stage-0 file."
    - "Mutations are contained, generation/OID checked, serialized, preflighted, rollback-capable, and Git-postcondition verified."
    - "A successful resolution offers Undo Resolution until Continue or operation generation change."
  artifacts:
    - path: src/main/conflictService.ts
      provides: Authoritative operation, conflict document, guarded apply, safety snapshots, and undo
    - path: src/main/__tests__/conflictService.test.ts
      provides: Real-repository security, operation transition, rollback, and undo matrix
    - path: src/main/index.ts
      provides: Sender/repository guarded purpose-specific IPC
  key_links:
    - from: src/main/conflictService.ts
      to: src/main/conflictRegions.ts
      via: Stage blobs become stable ConflictRegions and composed whole-file Results
    - from: src/main/index.ts
      to: src/main/conflictService.ts
      via: Validated narrow IPC handlers
---

<objective>
Implement authoritative, secure Git operation and conflict mutation APIs with visible resolution undo support.

Purpose: Make repeated rebase stops and all conflict mutations safe before renderer state or visual redesign depends on them.
Output: ConflictService, facade delegates, guarded IPC/preload contracts, real-repository tests, and generation-bound Undo Resolution.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-02-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement authoritative snapshots, documents, mutations, and undo</name>
  <files>src/main/conflictService.ts, src/main/git.ts, src/main/__tests__/conflictService.test.ts</files>
  <read_first>
    - src/main/git.ts
    - src/main/conflictRegions.ts
    - src/main/__tests__/conflictService.test.ts
    - src/shared/conflicts.ts
  </read_first>
  <behavior>
    - NUL-safe unmerged records and stage blobs produce OperationSnapshot and ConflictDocument, including structural/binary/symlink/submodule cases.
    - Apply validates generation, stage OIDs, region IDs, target path, result size/hash, and absence of unresolved choices before writing/staging a whole stage-0 file.
    - Continue/skip/abort use controlled noninteractive editor behavior and return completed, next-conflict, ready, or aborted snapshots.
    - Successful apply creates an Undo Resolution entry backed by exact before/after index/worktree safety state; undo restores and verifies the previous unmerged state before Continue.
  </behavior>
  <action>Create ConflictService behind gitService. Canonicalize repository/target real paths, reject traversal/absolute/symlink escape and non-member paths, use argument arrays plus `--`, impose payload/file limits, and serialize mutations per repository. Use stages 1/2/3 plus Plan 02 regions; never use working-tree markers as truth and never send conflict choices through ordinary partial patching. Capture recoverable index/worktree safety snapshots before apply, verify stage-0 and operation postconditions afterward, and roll back on any failure. Store generation-bound undo handles without embedding arbitrary renderer paths; expose Undo Resolution until Continue/generation transition, then expire it with a typed reason. Every operation command returns a fresh snapshot, including immediate subsequent conflicts.</action>
  <acceptance_criteria>
    - All characterized operation/conflict fixtures pass, including two rebase stops and abort restoration.
    - Structural/non-text cases are explicit file-level documents or typed unsupported cases.
    - Undo restores exact prior bytes/index entries and expires after Continue/generation change.
  </acceptance_criteria>
  <verify><automated>bun test src/main/__tests__/conflictService.test.ts src/main/__tests__/conflictRegions.test.ts</automated></verify>
  <done>Authoritative operations, complete-file resolution, rollback, postconditions, and Undo Resolution pass real-Git tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose guarded typed operation and undo IPC</name>
  <files>src/main/index.ts, src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/env.d.ts, src/main/__tests__/conflictService.test.ts</files>
  <read_first>
    - src/main/index.ts
    - src/preload/index.ts
    - src/preload/index.d.ts
    - src/renderer/src/env.d.ts
    - src/main/conflictService.ts
  </read_first>
  <behavior>
    - Only the trusted app frame and a canonical repository registered through open/restored-tab flow can invoke operations.
    - Snapshot, document, apply, undo resolution, continue, skip, and abort use purpose-specific typed methods.
    - Invalid sender/repository/path/enum/generation/OID/region/payload requests fail before mutation with stable codes.
  </behavior>
  <action>Add a shared privileged-handler guard for senderFrame/window ownership, repository registration/membership, canonical containment, request shape, byte limits, and expected identities. Register repositories through the trusted native open flow and restored-tab initialization after verifying the canonical path is a Git work tree. Preserve the response envelope with typed `code`; do not expose raw Git, filesystem access, patch strings, or ipcRenderer.</action>
  <acceptance_criteria>
    - Forged sender, unopened repo, traversal, symlink escape, stale identity, and oversized content tests prove no mutation.
    - The bridge contains only narrow methods and shared response types.
    - Undo accepts only an active opaque safety-snapshot handle for the matching repository/generation.
  </acceptance_criteria>
  <verify><automated>bun test src/main/__tests__/conflictService.test.ts && bun run build</automated></verify>
  <done>Every privileged conflict operation and Undo Resolution crosses one validated narrow boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer to main | Untrusted payload requests privileged Git/filesystem mutation. |
| UI snapshot to Git | External edits can stale generation, OIDs, regions, or undo handles. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-05 | Elevation of privilege | IPC | mitigate | Trusted sender and registered canonical repo guard. |
| T-06-06 | Tampering | Resolution | mitigate | Containment, expected identities, serialization, safety snapshots, postconditions. |
| T-06-07 | Denial of service | Payload | mitigate | File/content/region limits and binary fallback. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bun test src/main/__tests__/conflictService.test.ts && bun run build`</verification>
<success_criteria>Operations are authoritative and secure, repeated stops remain visible, and users can undo a resolution before continuation.</success_criteria>
## Artifacts this phase produces
- ConflictService and guarded bridge.
- Resolution safety snapshots and Undo Resolution.
- Real-Git security and transition tests.
<output>Create `06-03-SUMMARY.md` in this phase directory.</output>
