---
phase: 06-automatic-conflict-resolution-advanced-merging
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - src/renderer/src/store/conflictSession.ts
  - src/renderer/src/store/__tests__/conflictSession.test.ts
  - src/renderer/src/store/useRepoStore.ts
autonomous: true
requirements: [CONFLICT-01, CONFLICT-02]
must_haves:
  truths:
    - "Every conflict region begins unresolved; viewing never chooses content."
    - "Whole-file Result drafts survive navigation within one generation and stale events cannot overwrite a newer snapshot."
    - "One repository-scoped controller owns load/apply/undo/continue/skip/abort and external refresh."
    - "Undo Resolution is visible only while the active generation permits it."
  artifacts:
    - path: src/renderer/src/store/conflictSession.ts
      provides: Pure reducer/selectors for regions, whole-file Result, structural choices, external changes, and undo
    - path: src/renderer/src/store/useRepoStore.ts
      provides: Centralized repository operation controller
    - path: src/renderer/src/store/__tests__/conflictSession.test.ts
      provides: Pure state-machine coverage
  key_links:
    - from: src/renderer/src/store/useRepoStore.ts
      to: src/preload/index.ts
      via: Centralized typed operation actions consuming returned snapshots
---

<objective>
Build the safe renderer state/controller independently of the visual workbench.

Purpose: Remove hidden defaults and duplicated operation control while preserving drafts, external-change recovery, and resolution undo by generation.
Output: Pure conflict reducer/selectors, repository-scoped operation actions, and unit tests.
</objective>

<execution_context>@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-03-SUMMARY.md
@.planning/workstreams/mvp/phases/06-automatic-conflict-resolution-advanced-merging/06-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the pure explicit conflict session</name>
  <files>src/renderer/src/store/conflictSession.ts, src/renderer/src/store/__tests__/conflictSession.test.ts</files>
  <read_first>
    - src/renderer/src/store/useUndoStore.ts
    - src/renderer/src/store/__tests__/useUndoStore.test.ts
    - src/shared/conflicts.ts
  </read_first>
  <behavior>
    - Regions initialize unresolved and can choose current, incoming, both orders, selected range, manual, or reset.
    - A whole-file Result buffer is recomposed deterministically while navigation remains region-focused.
    - Structural/non-text files use file-level choices rather than text regions.
    - Generation replacement, stale events, repository isolation, external edits, dirty-draft recovery, and undo expiry are explicit transitions.
  </behavior>
  <action>Implement a pure reducer and selectors for active file/region, unresolved count, dirty Result, canApplyFile, canContinue, external-change comparison, and active Undo Resolution. Preserve drafts only within matching generation/document identity. Candidate events may be represented in contracts but remain inactive until Plans 09-10. Ensure Accept or viewing never implies Apply/Stage, and continuing or a new generation expires undo and stale drafts predictably.</action>
  <acceptance_criteria>
    - Tests cover every region and structural choice, both ordering rules, reset/manual edit, stale rejection, repository isolation, external edits, and undo visibility/expiry.
    - Tests require no window.api mock.
    - No selector treats marker absence alone as resolved.</acceptance_criteria>
  <verify><automated>bun test src/renderer/src/store/__tests__/conflictSession.test.ts</automated></verify>
  <done>The pure session has complete explicit transitions and no default side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize repository operation actions and authoritative refresh</name>
  <files>src/renderer/src/store/useRepoStore.ts, src/renderer/src/store/__tests__/conflictSession.test.ts</files>
  <read_first>
    - src/renderer/src/store/useRepoStore.ts
    - src/renderer/src/store/conflictSession.ts
    - src/preload/index.d.ts
  </read_first>
  <behavior>
    - Load/apply/undo/continue/skip/abort always dispatch the snapshot returned by main.
    - Watcher refresh updates the operation generation and surfaces external changes.
    - Consecutive conflicts with equal file counts still replace state by generation.
    - Apply performs one whole-file resolution call and exposes Undo Resolution before Continue.
  </behavior>
  <action>Integrate repository-keyed sessions into useRepoStore and make refreshRepo consume one OperationSnapshot rather than separate status/count inference. Add centralized actions for document loading, Apply and Stage, Undo Resolution, continue, skip, abort, minimize/open, and external refresh. Disable overlapping actions while pending, keep persistent errors, and never optimistically clear the session or resolved files.</action>
  <acceptance_criteria>
    - Components can consume selectors/actions without calling window.api directly.
    - Same-count next conflicts update by generation.
    - Undo refreshes authoritative document/snapshot and restored Result state.</acceptance_criteria>
  <verify><automated>bun test src/renderer/src/store/__tests__/conflictSession.test.ts && bun run build</automated></verify>
  <done>One repository-scoped controller owns all operation state and renderer mutations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Main snapshot to local draft | Authoritative Git state mixes with editable renderer state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-08 | Tampering | Draft/session | mitigate | Generation/document binding and explicit external-change recovery. |
| T-06-09 | Repudiation | Resolution choice | mitigate | Explicit unresolved/provenance state and pre-apply summary data. |
| T-06-SC | Tampering | Packages | accept | No package install. |
</threat_model>

<verification>`bun test src/renderer/src/store/__tests__/conflictSession.test.ts && bun run build`</verification>
<success_criteria>Renderer conflict state is explicit, repository-isolated, generation-aware, externally refreshable, and undo-aware.</success_criteria>
## Artifacts this phase produces
- Pure conflict session reducer/selectors.
- Central operation controller and undo lifecycle.
<output>Create `06-04-SUMMARY.md` in this phase directory.</output>
